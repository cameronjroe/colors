# colors

> Infinite random color generator.

Yield random colors (hex codes) infinitely.

## Usage

```js
import colors from 'colors';

var gen = colors();

var color1 = gen.next();
var color2 = gen.next();
...

```
